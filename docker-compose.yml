services:
  web:
    build: .
    restart: unless-stopped
    command: /app/entrypoint.sh
    volumes:
      - .:/app
      - /app/backend/staticfiles
    ports:
      - "8000:8000"
    env_file:
      - .env